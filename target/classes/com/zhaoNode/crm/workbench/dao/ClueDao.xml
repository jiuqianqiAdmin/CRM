<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zhaoNode.crm.workbench.dao.ClueDao">
    <select id="getChart" resultType="Map">
        select count(*) as value ,state as name from tbl_clue group by state
    </select>
    <select id="getClue" resultType="Clue">
        select * from tbl_clue where id=#{clueId}
     </select>
    <delete id="deleteClueById">
        delete from tbl_clue where id in
        <foreach collection="array" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>
    <update id="updateClue">
        update tbl_clue set
        fullname=#{fullname},
        appellation=#{appellation},
        owner=#{owner},
        company=#{company},
        job=#{job},
        email=#{email},
        phone=#{phone},
        website=#{website},
        mphone=#{mphone},
        state=#{state},
        source=#{source},
        editBy=#{editBy},
        editTime=#{editTime},
        description=#{description},
        contactSummary=#{contactSummary},
        nextContactTime=#{nextContactTime},
        address=#{address}
        where id=#{id}

    </update>
    <select id="getClueById" resultType="Clue">
        select * from tbl_clue where id=#{id}
    </select>
     <select id="detail" resultType="Clue">
         select
          c.id,
          c.fullname,
          c.appellation,
          u.name as owner,
          c.company,
          c.job,
          c.email,
          c.phone,
          c.website,
          c.mphone,
          c.state,
          c.source,
          c.createBy,
          c.createTime,
          c.editBy,
          c.editTime,
          c.description,
          c.contactSummary,
          c.nextContactTime,
          c.address
          from
          tbl_clue c join tbl_user u
          on c.owner = u.id
          where c.id=#{id}
     </select>
     <select id="getClueByCondition" resultType="Clue">
          select
          c.id,
          c.fullname,
          c.appellation,
          u.name as owner,
          c.company,
          c.job,
          c.email,
          c.phone,
          c.website,
          c.mphone,
          c.state,
          c.source,
          c.createBy,
          c.createTime,
          c.editBy,
          c.editTime,
          c.description,
          c.contactSummary,
          c.nextContactTime,
          c.address
          from
          tbl_clue c join tbl_user u
          on c.owner = u.id
          <where>
               <if test="fullname!=null and fullname!=''">
                    c.fullname like '%' #{fullname} '%'
               </if>
               <if test="createBy!=null and createBy!=''">
                    and c.createBy like '%' #{createBy} '%'
               </if>
               <if test="source!=null and source!=''">
                    and  c.source = #{source}
               </if>
               <if test="company!=null and company!=''">
                    and c.company like '%' #{company} '%'
               </if>
               <if test="phone!=null and phone!=''">
                    and c.phone like '%' #{phone} '%'
               </if>
               <if test="mphone!=null and mphone!=''">
                   and c.mphone like '%' #{mphone} '%'
               </if>
               <if test="state!=null and state!=''">
                    and c.state = #{state}
               </if>
          </where>
          order by c.createTime desc
          limit #{skipCount},#{pageSize}
     </select>
     <select id="getTotalByCondition" resultType="int">
          select count(*)
          from
          tbl_clue c join tbl_user u
          on c.owner = u.id
          <where>
               <if test="fullname!=null and fullname!=''">
                    c.fullname like '%' #{fullname} '%'
               </if>
               <if test="createBy!=null and createBy!=''">
                    and c.createBy like '%' #{createBy} '%'
               </if>
               <if test="source!=null and source!=''">
                    and  c.source = #{source}
               </if>
               <if test="company!=null and company!=''">
                    and c.company = #{company}
               </if>
               <if test="phone!=null and phone!=''">
                   and c.phone like '%' #{phone} '%'
               </if>
               <if test="mphone!=null and mphone!=''">
                   and c.mphone like '%' #{mphone} '%'
               </if>
               <if test="state!=null and state!=''">
                    and c.state = #{state}
               </if>
          </where>

     </select>
    <insert id="save">
        insert into tbl_clue
        (
             id,
             fullname,
             appellation,
             owner,
             company,
             job,
             email,
             phone,
             website,
             mphone,
             state,
             source,
             createBy,
             createTime,
             description,
             contactSummary,
             nextContactTime,
             address
        )
        values
        (
             #{id},
             #{fullname},
             #{appellation},
             #{owner},
             #{company},
             #{job},
             #{email},
             #{phone},
             #{website},
             #{mphone},
             #{state},
             #{source},
             #{createBy},
             #{createTime},
             #{description},
             #{contactSummary},
             #{nextContactTime},
             #{address}
        )
    </insert>
</mapper>

































